import React, {Component} from 'react';
import PropTypes from 'prop-types';

export default class Homepage extends Component {
  static propTypes = {

  };

  render() {
    return (
      <h3>Welcome to the homepage! This is where blog posts and a search bar will go!</h3>
    );
  }
}